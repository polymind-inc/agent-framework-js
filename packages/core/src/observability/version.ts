/**
 * Version reported as the OpenTelemetry instrumentation scope of every span and
 * metric this package emits.
 *
 * Kept in sync with the `version` field in package.json; a unit test enforces
 * the match.
 */
export const INSTRUMENTATION_VERSION = '0.2.2';
